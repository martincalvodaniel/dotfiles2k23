#!/bin/bash
# Link custom dotfiles
cp "$DOTS_HOME/homeFiles/.aliases.zsh" ~/.aliases.zsh
cp "$DOTS_HOME/homeFiles/.p10k.zsh" ~/.p10k.zsh
cp "$DOTS_HOME/homeFiles/.zshrc" ~/.zshrc
cp "$DOTS_HOME/homeFiles/.zshenv" ~/.zshenv
